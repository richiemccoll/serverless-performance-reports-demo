import React from 'react';
import ReactDOM from 'react-dom';

function App() {
 return <main>Serverless Performance Reports </main>
}

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
